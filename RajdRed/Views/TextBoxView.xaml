<UserControl x:Class="RajdRed.Views.TextBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="10" d:DesignWidth="30"
             MouseEnter="UserControl_MouseEnter"
             MouseMove="UserControl_MouseMove"
             >
    <Grid>
        <Border Name="TextBoxBorder" BorderThickness="2" 
                BorderBrush="Black"
                Padding="5"
                Visibility="{Binding TextBoxModel.Visible}"/>
        <TextBox DataContext="{Binding}" 
                 BorderThickness="0"
                 Background="Transparent"
                 TextWrapping="Wrap"
                 Text="{Binding TextBoxModel.Text}"
                 GotFocus="TextBox_GotFocus"/>
    </Grid>
</UserControl>
